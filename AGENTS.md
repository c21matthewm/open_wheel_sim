# Project Working Instructions

`PROJECT_STATE.md` is the canonical description of the project's current
behavior, architecture, build/run workflow, verification, and limitations.

Any change that alters those areas must update `PROJECT_STATE.md` in the same
change. Keep unverified hardware behavior clearly labeled.

Keep `initial_prompt_instructions.txt` as the retained original project brief.
